apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - mongodb-svc.yaml
  - mongodb-ss.yaml
  
commonLabels:
  app: mongodb
  project: proj-gke

configMapGenerator:
  - name: mongodb-init-colordb
    files:
      - mongodb-init.js
  